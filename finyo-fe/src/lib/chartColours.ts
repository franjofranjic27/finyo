/**
 * Shared categorical palette for charts (donuts, swatches).
 * Entries are cycled with `CHART_COLOURS[i % CHART_COLOURS.length]`.
 */
export const CHART_COLOURS = ['#6366f1', '#8b5cf6', '#a78bfa', '#c4b5fd', '#ddd6fe', '#e0e7ff'];
